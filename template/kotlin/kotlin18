try {
            var flightworkstationnes = "flightworkstationnes provisooutgoing"
            val militiasalvepause = StringBuffer()

            if (flightworkstationnes.length == 1) {
            }
        } catch (camerastruggle: java.lang.Exception) {
            camerastruggle.printStackTrace()
        }